{% extends 'base.html.twig' %}

{% block title %}Liste des Auteurs{% endblock %}
{% block nom_page %}Liste des Auteurs{% endblock %}
{% block body %}
            <ul>
                {% for auteur in auteurs %}
                    <a href="{{ path('auteur_view_name', {'name': auteur.name})}}"><li>Nom : {{auteur.name}}</li></a>
                {% else %}
                    <li>Pas d'Auteur</li>
                {% endfor %}
            </ul>
            {# <a href="{{ path('auteur_add')}}"><button>Ajouter un auteur</button></a> #}
            <a href="#"><button>Ajouter un auteur</button></a>
{% endblock %}
