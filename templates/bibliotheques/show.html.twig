{% extends 'base.html.twig' %}

{% block title %}Bibliotheques{% endblock %}

{% block body %}
    <h1>Bibliotheques</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bibliotheque.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bibliotheque.name }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ bibliotheque.city }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ bibliotheque.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bibliotheques_index') }}">back to list</a>

    <a href="{{ path('bibliotheques_edit', {'id': bibliotheque.id}) }}">edit</a>

    {{ include('bibliotheques/_delete_form.html.twig') }}
{% endblock %}
